<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.tuempresa.tuapp">

  <application
      android:label="Mi App"
      android:icon="@mipmap/appicon"
      android:theme="@style/AppTheme">

    <!-- … otras actividades que ya tengas … -->

    <!-- WebAuthenticator deep‐link handler -->
    <activity
    android:name="microsoft.maui.Authentication.WebAuthenticatorCallbackActivity"
    android:exported="true">
  <intent-filter>
    <action android:name="android.intent.action.VIEW" />
    <category android:name="android.intent.category.DEFAULT" />
    <category android:name="android.intent.category.BROWSABLE" />

    <!-- Intercepta el callback HTTPS de Supabase -->
    <data
      android:scheme="https"
      android:host="vtraxawdyzwgujhlcdyf.supabase.co"
      android:pathPrefix="/auth/v1/callback" />
  </intent-filter>
</activity>


  </application>
</manifest>
